{
  "name": "moduli-foundation",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web-foundation dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
